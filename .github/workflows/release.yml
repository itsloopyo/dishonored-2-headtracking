name: Release
run-name: "Release ${{ github.ref_name }}"

# Everything this release does lives in cameraunlock-core's release-mod.yml.
# Only the values below are this mod's. Pinned to a commit SHA, never a branch:
# that workflow runs with contents:write, the Discord webhook and the Lopari PAT.

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  release:
    # 2026-08-30 - release-mod.yml, the fleet's one reusable release workflow.
    # cameraunlock-core carries no tags, so the date and subject stand in for the
    # version comment the pin would otherwise name.
    uses: itsloopyo/cameraunlock-core/.github/workflows/release-mod.yml@435915a8a3d8e1b799cc169b74f1fcf72b9534e7
    with:
      project-name: 'Dishonored 2 Head Tracking'
      version-source: manifest
      version-path: 'manifest.json'
      artifact-paths: 'src/ cameraunlock-core/ scripts/install.cmd scripts/uninstall.cmd'
      setup-msbuild: true
    permissions:
      contents: write
    secrets: inherit
